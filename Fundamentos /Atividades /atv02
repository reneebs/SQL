DROP DATABASE IF EXISTS clientes;
CREATE DATABASE clientes;
USE clientes;

-- Criação da tabela clientes
CREATE TABLE clientes (
    idcliente INT PRIMARY KEY,
    nome VARCHAR(100),
    email VARCHAR(100),
    cpf BIGINT
);

-- Alteração para adicionar a coluna telefone
ALTER TABLE clientes
ADD telefone VARCHAR(20);

-- Inserção de dados em clientes (com o campo telefone incluído)
INSERT INTO clientes VALUES
(1, 'rené', 'rene@gmail.com', 12345678901, '11999998888'),
(2, 'jesus', 'jesus@gmail.com', 98765432100, '11977776666');

SELECT * FROM clientes;

-- Criação da tabela pedidos
CREATE TABLE pedidos (
    idpedidos INT PRIMARY KEY,
    idcliente INT,
    data_pedido DATE,
    valor_total DECIMAL(10,2),
    FOREIGN KEY (idcliente) REFERENCES clientes(idcliente)
);

-- Inserção de dados em pedidos
INSERT INTO pedidos VALUES
(1, 1, '2026-09-02', 50.00),
(2, 2, '2026-09-02', 80.50);

SELECT * FROM pedidos;

SHOW TABLES;

-- DROP TABLE pedidos;
