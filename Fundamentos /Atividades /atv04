CREATE DATABASE IF NOT EXISTS db_livraria;
USE db_livraria;

CREATE TABLE autor (
    id_autor INT PRIMARY KEY AUTO_INCREMENT,
    nome VARCHAR(100) NOT NULL,
    nacionalidade VARCHAR(50),
    data_nascimento DATE
);

CREATE TABLE editora (
    id_editora INT PRIMARY KEY AUTO_INCREMENT,
    nome VARCHAR(100) NOT NULL,
    endereco VARCHAR(200),
    telefone VARCHAR(30),
    email VARCHAR(100)
);

CREATE TABLE livro (
    id_livro INT PRIMARY KEY AUTO_INCREMENT,
    titulo VARCHAR(200) NOT NULL,
    ano_publicacao YEAR,
    id_editora INT,
    id_autor_principal INT,
    FOREIGN KEY (id_editora) REFERENCES editora(id_editora),
    FOREIGN KEY (id_autor_principal) REFERENCES autor(id_autor)
);

INSERT INTO autor (nome, nacionalidade, data_nascimento)
VALUES
('Machado de Assis', 'Brasil', '1839-06-21'),
('Jorge Amado', 'Brasil', '1912-08-10'),
('C. K. Rolling', 'Inglaterra', '1965-07-31');

INSERT INTO editora (nome, endereco, telefone, email)
VALUES
('Editora alfa', 'Rua A, 100', '71999999999', 'alfa@email.com'),
('Editora beta', 'Rua B, 200', '71888888888', 'beta@email.com');

INSERT INTO livro (titulo, ano_publicacao, id_editora, id_autor_principal)
VALUES
('Dom Casmurro', 1899, 1, 1),
('Capitães da Areia', 1937, 2, 2);

UPDATE editora
SET telefone = '7167676767'
WHERE id_editora = 1;

SELECT * FROM autor;

SELECT titulo, ano_publicacao
FROM livro;
